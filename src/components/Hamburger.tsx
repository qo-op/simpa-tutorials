import React from "react";

class Hamburger extends React.Component {
  render = () => {
    return (
      <div className="Hamburger BorderLayout">
        <span className="material-icons">menu_open</span>
      </div>
    );
  };
}

export default Hamburger;
